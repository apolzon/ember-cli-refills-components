import Ember from 'ember';

RefillsAccordionTabsItemComponent = Ember.Component.extend({
  tagName: "li",
  classNames: ["tab-header-and-content"]
});

export default RefillsAccordionTabsItemComponent;
